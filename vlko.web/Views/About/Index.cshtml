@using vlko.BlogModule.Commands.ViewModel
@model StaticTextWithFullTextViewModel

@{
	ViewBag.Title = "About us";
}
<h2 class="note-main">@Model.Title</h2>
<article class="one-note">
	<div class="info">
		<div class="last-change-date" title="Last change date">@String.Format("{0:g}", Model.ChangeDate)</div>
	</div>
	<div class="text">
		@Html.Raw(Model.Text)
	</div> 
</article>

